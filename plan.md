# Plan — crosspec

Living roadmap. Authoritative north star is `direction.md`; this file tracks **current state**.

## Current milestone

**Phase 1 — Ship configurator engine v0.1 with smart home as first vertical instance (Week 3).** Research phase is closed. Vertical 1 committed: smart home (Matter/Thread/Zigbee). Solar on deck as Vertical 2. Goal now: build the **generalizable configurator engine** (constraint solver, LLM orchestration, affiliate routing, UI shell) and drop smart home in as the first vertical instance. Not a standalone smart-home product. The engine lives in one place from day one; `verticals/smart-home/` is a thin wrapper (knowledge base + affiliate mappings + prompts + copy). `verticals/solar/` should be an empty stub that documents the contract — if adding solar later requires changing engine code, that's a design failure per the hard rule. Build cost is LLM-assisted hours; cost of picking wrong is tiny vs. cost of research paralysis. Four days without a line of code is the failure mode — reset.

Domain `crosspec.com` is live on Cloudflare DNS with DNSSEC signed (Namecheap registrar, expires 2027-04-20, auto-renew on). Cloudflare zone ID `72048f516d550fc95de1b6a1181246bd`. Repo currently holds `direction.md`, this file, `CLAUDE.md`, `.claude-flow` marker, and the promoted SERP-check tooling at `tools/serp-check/`. No code yet.

## Branch strategy
No branches in use yet — directory is fresh. Adopt `develop` → `main` pattern once Phase 1 code lands (from `~/projects/synoros-platform` prior art). Revisit this section then; for now plan/doc edits commit straight to `main` (or `master`, whichever `git init` defaults to).

## In progress

**Build-phase items only. No more research/triage items without explicit founder request.**

- [ ] **Affiliate partner applications for smart home** — DoD: applications submitted to SwitchBot (Awin, 6-15%/60d — highest-value anchor), TP-Link Tapo (direct 10%), Govee (Impact, 5-10%), Philips Hue/Signify (Awin/Partnerize), Aqara (Awin UK, Sovrn US). Track approval state in `~/notes/projects/crosspec-affiliate-status.md`. Amazon Associates fallback for anything not approved or SKU-missing. Parallel with build — can start today while MVP is being written.

- [ ] **Community-first acquisition plan (post-ship)** — DoD: short list of where to post the v0.1 configurator once it's live — r/homeautomation, r/smarthome, r/matterandthread, HA Discord + community forum, relevant subreddits for specific ecosystems. NOT a launch plan with hype — a genuine "I made this thing that might help" post from the EEAT author account. Written up in `~/notes/projects/crosspec-community-plan.md`. 45 min.

## Recent completions

- 2026-04-23 — **smart_lock device type added to smart-home vertical (4th device type).** 4 KB entries (Aqara U100 Matter wifi/HomeKey, Yale Assure 2 Matter thread/Matter, Schlage Encode Plus Matter wifi/HomeKey, August Wi-Fi 4th gen non-Matter wifi) covering Matter-over-Wi-Fi, Matter-over-Thread, and non-Matter protocol paths. ConfiguratorShell + `engine/ui/render.ts` grow matching `lockPicks` filter + "Smart locks" render section, TYPE_KEYWORDS covers "smart lock" / "deadbolt" / "door lock", one HomeKey example chip on /smart-home. Tests 27 → 33 green (3 solver + 3 render). Zero engine source changes — the 7-touchpoint slice contract holds for the third type in a row. Commit `e0bbdab`.

- 2026-04-23 — **foundry-01 dev fabric wired for crosspec.** `crosspec.foundry.test` serves `astro dev --port 3005` live from the workstation via mutagen sync (`/home/jack/projects/crosspec` ↔ `jack@10.0.0.3:/srv/dev/crosspec`, ignoring `node_modules` / `dist` / `.astro`) + systemd user unit `crosspec-dev.service` + Caddy site block reverse-proxying `localhost:3005`. Local edits hot-reload on foundry. `astro.config.mjs` now lists `crosspec.foundry.test` in `vite.server.allowedHosts` — Vite's default host check rejects non-localhost proxied hostnames with 403.

- 2026-04-22 — **EEAT author page live at `/about`.** Named author (Jack), 1-paragraph hands-on credibility hook (Home Assistant + Matter/Thread/Zigbee across Hue, Aqara, SwitchBot, Tapo, Eve), attribution / affiliate disclosure paragraph, no LinkedIn link (founder choice — credibility comes from the named-author + concrete claims). Commits `c742bf3` (scaffold) + `b8f0e27` (LinkedIn strip).

- 2026-04-22 — **smart_plug device type added to smart-home vertical.** KB expanded from 2 to 3 device types: 5 smart plug entries (Tapo P100, Tapo P125M Matter, Amazon Smart Plug, SwitchBot Plug Mini W1901400, Eve Energy Matter) with source citations matching the existing attribution table. ConfiguratorShell gains a smart_plug checkbox + plugPicks render block, `engine/ui/render.ts` mirrors in the hydrated client path, TYPE_KEYWORDS covers "smart plug"/"outlet", one plug example chip on /smart-home. Tests 21 → 27. Zero engine changes — the expansion proves the KB-and-UI-wrapper contract works. Slots left for adding a 4th type (smart lock, climate sensor, etc.) the same way.

- 2026-04-22 — **OG + Twitter meta + canonical links.** Shared URLs on Reddit / Discord / HA forum now get proper previews. `Layout.astro` emits `og:title/description/url/site_name`, `twitter:card`, and a `rel=canonical` on every page. URL comes from `Astro.url.pathname` so `crosspec.com/<route>/` is the canonical regardless of which host served it.

- 2026-04-22 — **Affiliate clickout via Pages Functions.** `functions/go/[vertical]/[sku].ts` looks up SKU in `verticals/<vertical>/affiliates.json.products`, substitutes `{{merchant_sku}}` + `{{affiliate_tag}}` into the merchant's `link_template`, 302s. No product match → Amazon search fallback. Handles GET + HEAD. Merchant tags read from `MERCHANT_TAG_*` env bindings on the Pages project (none set yet — all clicks currently hit untagged Amazon search, which is expected).

- 2026-04-22 — **Copy-link button + shareable URLs via hash.** Configurator state (ecosystem + wants) persists in `location.hash` via `encodeInputs/decodeInputs` (URLSearchParams form). On load → decode hash → populate form → re-solve. On form change → `history.replaceState` with new hash. `hashchange` listener re-reads on paste. Copy-link button near the form copies `location.href` and shows "Link copied" for 2s. Directly implements direction.md's "saved configurator URLs as long-tail UGC" acquisition strategy.

- 2026-04-22 — **Interactive configurator shipped.** `/smart-home` page now has a working form (ecosystem radio + device-type checkboxes) and clickable example chips that keyword-match the text, populate the form, and re-solve client-side. `engine/ui/render.ts` renders solver output → HTML string; re-render happens on any form change. Closed the "Tools, not content" hard-rule gap (page no longer advertises "v0.2 coming"). 8 tests for render + matchExample.

- 2026-04-22 — **v0.1 deployed end-to-end.** GitHub repo `superninjv/crosspec` live, Cloudflare Pages project `crosspec` created, CF API token + account ID wired as repo secrets, deploy workflow green on first push + second workflow_dispatch run. Both `/` and `/smart-home/` served at `crosspec.pages.dev` and custom-attached `crosspec.com`. Commits 1–3 (scaffolding, solver, workflow) + commits 4–5 (interactive form + deploy).

- 2026-04-21 — **Research phase closed. Committed: smart home V1, build-phase starts next session.** Pattern observed: four days of triage/pressure-test/kill loops without a line of code shipped. The triage surfaced real information (keyboards killed on Drop shutdown + KeebFinder dominance, solar has clean data pipeline, smart home has diversified affiliate programs) but the gate-stacking was the failure mode, not the picking. AIO behavior check on smart home returned 3/3 complete AIOs on compat + best-X-for-Y + multi-constraint-product queries. Interpretation: AIO eats top-of-funnel SEO for configurator verticals in 2026 as a CATEGORY pattern, not a smart-home-specific issue. Solar would show the same. Correct response: NOT a kill, it's a go-to-market signal — pivot acquisition strategy to community-first (r/homeautomation + HA Discord + Matter subreddit), configurator-as-destination, saved-configurator-URLs as long-tail UGC. SEO top-of-funnel is not the engine. Full AIO data at `~/notes/projects/crosspec-aio-check.md`. Memories saved: `feedback_ship_dont_triage.md`, `feedback_aio_is_signal_not_gate.md`.

- 2026-04-21 — **Vertical 1 committed: smart home (Matter/Thread/Zigbee ecosystem configurator). Vertical 2 on deck: solar+battery+inverter sizing.** Founder commitment: "crosspec Vertical 1 is a multi-vendor smart-home ecosystem configurator covering Matter / Thread / Zigbee / cloud-ecosystem compatibility with affiliate-routed product recommendations; solar is Vertical 2 once smart home is earning." Rationale: smart home wins 4 of 6 triage comparison factors over solar (affiliate diversification, EEAT ramp, competitor landscape, AIO resistance). Next: run the 15-query AIO behavior check as the Phase 0 go/no-go gate. If AIO check fails, pivot to solar (which also passed viability).

- 2026-04-21 — **Solar + smart home both triaged; smart home is the stronger recommendation** (awaiting founder commit). Solar = VIABLE WITH CAVEATS: strong data pipeline (CEC CSV + pvlib + NREL PVWatts) but affiliate anchor is Project Solar's $500/install which conflicts with EnergySage's install-intent turf, hardware affiliate rates are thin (5-8% on $1k AOV), YMYL-adjacent EEAT ramp is 6-12 months, and multiple 2025 "AI solar sizer" entrants are already fighting for positioning. Smart home = VIABLE WITH CAVEATS: diversified direct affiliate programs (SwitchBot 6-15%/60d, Tapo 10%, Govee 5-10%, Hue via Awin), permissive data (HA integrations + Z2M + CSA Matter 13k+ entries), genuinely greenfield competitor landscape (HA device DB arriving H1 2026 but explicitly non-recommender), AIO-resistant query shape (ecosystem-dependent compatibility), non-YMYL so EEAT ramp is weekends not months. Smart home wins 4 of 6 comparison factors. Full notes at `~/notes/projects/crosspec-vertical-triage.md`.

- 2026-04-21 — **Mechanical keyboards RULED OUT as Vertical 1** by pre-AIO viability pressure-test. Three independent failure signals: (1) Drop.com shut down 2026-03-31 — Corsair killed their e-commerce, eliminating the anchor multi-brand affiliate partner; (2) KeebFinder (keeb-finder.com) already owns the space at ~300k PV/mo with 4,000+ keyboards, 8,500+ components, 40+ affiliate vendor integrations — a solo 6-month build lands where they were in 2023; (3) Amazon Associates keyboards rate is 2.5% with a 24-hour cookie, single-brand programs (Keychron, Lofree at 15%) can't carry a multi-brand hub. AIO behavior check not run — gates 1 and 2 killed the vertical first. Full triage at `~/notes/projects/crosspec-vertical-triage.md`. Lessons for next triage: verify affiliate partners are still operating (not just listed), use competitor pageviews (not feature counts) to gauge market saturation.

- 2026-04-20 — **Project kicked off.** `~/projects/crosspec/` directory created, `.claude-flow` marker + `direction.md` + `plan.md` + `CLAUDE.md` authored. Domain `crosspec.com` carried forward from the killed crossref project (Namecheap registrar, Cloudflare DNS with DNSSEC signed, expires 2027-04-20, auto-renew on). Thesis: multi-vertical configurator hub, vertical-first execution discipline, destination-but-not-starting-point hub branding.

## What we are explicitly NOT doing (preserve between sessions)

- Not launching the hub homepage yet. Index stays Vertical 1 focused until Phase 4 criteria met.
- Not building a generic "configurator framework" as a product. The shared engine exists for internal reuse across our own verticals; it is not a B2B developer product.
- Not starting two verticals simultaneously. Vertical 2 is gated on Vertical 1 earning affiliate revenue.
- Not scraping sites with hostile ToS. If Vertical 1's data access depends on an adversarial source (looking at you, PCPartPicker), we pick a different vertical.
- Not building display-ad-monetized content. Every page is a working tool, or we got it wrong.
